{
  "collectionName": "components_sections_advantage_cards",
  "info": {
    "displayName": "Advantage Card",
    "icon": "grid",
    "description": "Core Advantages — single card (title + description)"
  },
  "options": {},
  "attributes": {
    "iconId": {
      "type": "enumeration",
      "enum": ["clock", "lock", "globe"],
      "default": "clock"
    },
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text"
    },
    "cardLink": {
      "type": "string"
    }
  }
}
